//Dias em horas
//Crie um algoritmo que converte dias em horas, quando recebe um número de dias. 

function conversorTempo(dias) {
    let conversor = dias * 24;
    console.log(`${dias} dia(s) equivalem a ${conversor} hora(s)`);
}
conversorTempo(3);