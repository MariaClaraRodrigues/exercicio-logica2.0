//Quero somar: 
//Faça um programa que peça dois números e imprima no console a soma desses dois números.

function soma(a,b) {
    let res = a + b;
    console.log(`A soma do valor 1: ${a} + valor 2: ${b} = ${res}`);
}
soma(5, 10)