//Multiplica
//Crie uma função que recebe 2 parâmetros e retorna a multiplicação entre else.

function multiplica(num1,num2) {
    let res = num1 * num2;
    console.log(`A multiplicação de ${num1} x ${num2} = ${res}`);
    return res;
}
multiplica(5,10);